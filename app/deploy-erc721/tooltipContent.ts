export const MetadataExtensionContent =
  'If your IPFS metadata files are formatted with a json extension (1.json), select ".json". If your metadata files are formatted without a json extension (1), select "none".';
export const MetadataURIContent =
  'You can get the URI (or CID) for your metadata files from your IPFS pinning service. Make sure to include the correct prefix (ipfs://) and a trailing slash (/) so your metadata is referenced correctly.';
